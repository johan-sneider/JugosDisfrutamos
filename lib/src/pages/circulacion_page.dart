import 'package:flutter/material.dart';

class CirculacionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Circulacion Page'),),
      body: Text('Circulacion Page'),
    );
  }
}