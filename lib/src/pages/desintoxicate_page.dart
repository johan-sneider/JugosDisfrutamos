import 'package:flutter/material.dart';

class DesitoxicatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Desintoxicate Page'),),
      body: Text('Desintoxicate Page'),
    );
  }
}