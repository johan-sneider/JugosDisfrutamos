import 'package:flutter/material.dart';

class InmunologicoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Inmunologico Page'),),
      body: Text('Inmunologico Page'),
    );
  }
}